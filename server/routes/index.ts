import fundraiser from './fundraiser.route'
import category from './category.route'
import user from './user.route'
import code from './code.route'

const routers = {
  fundraiser,
  category,
  user,
  code
}

export default routers